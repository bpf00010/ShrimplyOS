# 🦐 Shrimply OS Minimal Kernel Config Placeholder 🦐
# This file should be populated with a minimal `make menuconfig` output
# tailored specifically for the target hardware to reduce boot time and memory footprint.
CONFIG_64BIT=y
CONFIG_X86_64=y
CONFIG_SMP=y
# ... (Full configuration omitted for brevity, generate via `make defconfig` and trim)
